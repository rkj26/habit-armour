{
  "name": "auto-adder-backend",
  "version": "1.0.0",
  "description": "Backend for habits tracker and device locker",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore habits_data.json --ignore *.log --ignore *.err server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
